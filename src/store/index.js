import { createStore } from "vuex";
import menuState from "./state/menus";
export default createStore({
    state: {
        menuState,
    },
    getters: {},
    mutations: {},
    actions: {},
    modules: {},
});
